/**
 * Model page for PaleoCAR model.
 */

import {
  connect,
} from 'react-redux';

import Component from './component';

export default connect(
  // mapStateToProps
  null,
  // mapDispatchToProps
  null,
)(Component);
